const host = "http://dev.autopc.top:8085";
const imgPreviewImgPath = `${host}/api/previewImg`;
const publicPath=`${host}/public`;
const imgSourcePath=`${publicPath}/img`;
export {
    host,
    publicPath,
    imgPreviewImgPath,
    imgSourcePath
}